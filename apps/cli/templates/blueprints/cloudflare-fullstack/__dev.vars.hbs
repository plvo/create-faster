---
mono:
  scope: app
  path: .dev.vars
---
# Local secrets for `bun run preview` (OpenNext) and `wrangler dev`.
# Production values are set with `wrangler secret put`. This file is gitignored.
BETTER_AUTH_SECRET=dev-secret-change-me
BETTER_AUTH_URL=http://localhost:8787
