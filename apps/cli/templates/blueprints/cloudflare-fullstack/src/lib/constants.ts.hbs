---
mono:
  scope: app
  path: src/lib/constants.ts
---
import type { LucideIcon } from 'lucide-react';
import { FileText, ShieldCheck, Users } from 'lucide-react';

export interface RouteProps {
  url: string;
  title: string;
  icon?: LucideIcon;
  category?: string;
  permissions?: Record<string, string[]>;
}

export const ROUTES: RouteProps[] = [
  { url: '/', title: 'Documents', icon: FileText },
  { url: '/admin/users', title: 'Users', icon: Users, category: 'Administration', permissions: { user: ['list'] } },
  { url: '/admin/roles', title: 'Roles', icon: ShieldCheck, category: 'Administration', permissions: { user: ['list'] } },
];
