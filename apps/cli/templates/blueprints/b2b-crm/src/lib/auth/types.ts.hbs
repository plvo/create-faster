---
mono:
  scope: pkg
  name: auth
  path: src/types.ts
path: src/lib/auth/types.ts
---
import type { auth } from {{#if (isMono)}}'./auth'{{else}}'@/lib/auth/auth'{{/if}};

export type Session = typeof auth.$Infer.Session;
