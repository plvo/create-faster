import { render, screen } from '@testing-library/react-native';
import { Text } from 'react-native';

describe('Greeting', () => {
  it('renders text', () => {
    render(<Text>Hello {{projectName}}</Text>);
    expect(screen.getByText('Hello {{projectName}}')).toBeTruthy();
  });
});
