Local SQLite file database, no server or container needed. The file path comes from `DATABASE_URL` (see `.env.example`); `{{pm}} db:push` creates the file automatically. The Drizzle client uses the `@libsql/client` driver, which runs on any JavaScript runtime (Node and Bun).
