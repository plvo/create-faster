{{!-- @dest:app --}}
{{#if (eq repo "single")}}
import { toNextJsHandler } from 'better-auth/next-js';
import { auth } from '@/lib/auth/auth';

export const { GET, POST } = toNextJsHandler(auth.handler);
{{else}}
export { GET, POST } from '@repo/auth/route';
{{/if}}
