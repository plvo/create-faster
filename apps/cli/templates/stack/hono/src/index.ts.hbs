{{#if (hasModule "aws-lambda")}}
import { handle } from 'hono/aws-lambda';
{{/if}}
import app from './app';

{{#if (hasModule "aws-lambda")}}
export const handler = handle(app);
{{else}}
export default app;
{{/if}}