{{#if (has "module" "aws-lambda")}}
import { handle } from 'hono/aws-lambda';
{{/if}}
import app from './app';

{{#if (has "module" "aws-lambda")}}
export const handler = handle(app);
{{else}}
export default app;
{{/if}}