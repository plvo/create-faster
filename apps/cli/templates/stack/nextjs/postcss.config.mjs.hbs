{{#if (and (isMono) (hasLibrary "shadcn"))}}
export { default } from '@repo/ui/postcss.config.mjs';
{{else}}
const config = {
  plugins: ["@tailwindcss/postcss"],
  };

export default config;
{{/if}}
